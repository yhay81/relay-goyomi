# Decisions

## 2026-07-30

- `relay-goyomi.yhay81.com` で、yhay81の個人プロジェクトとして公開する。
- 7〜31日の記事リレーを作り、参加者が日付を予約して後から外部記事URLを付ける用途に絞る。
- 公開ページは閲覧専用にし、予約には別の参加URLを必要とする。
- Better Authを使わず、主催者・参加・各枠編集の256-bit能力URLで管理する。
- 能力鍵はURLフラグメントに置き、サーバーにはSHA-256ハッシュだけを保存する。
- 公開ディレクトリ、検索、コメント、連絡先、画像、アップロード、埋め込み、HTML、Markdownを設けない。
- 記事URLはHTTPSだけを許可し、認証情報、非標準ポート、ローカル・プライベート宛てを拒否する。
- 異なる3ブラウザーかつ3接続元からの報告で自動非表示にし、主催者の修正保存時は報告履歴を消して再公開する。
- 接続元IPは保存せず、カレンダー単位の秘密鍵付きHMACだけで重複報告を抑える。
- 非表示のカレンダーは30日後、匿名操作ログは45日後に削除する。
- UIは大きな文字ではなく、日付札とそれらを結ぶリボン、空き・予約・公開の状態差で用途を示す。
